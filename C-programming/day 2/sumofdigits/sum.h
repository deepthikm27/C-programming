// Enter a 5 digit number and find the sum of its digits
int sum_of_five_digits(int);

//to reverse the number
int findReverse(int);

// to count the occurrences of digit in a number.
int countoccurence(int , int);

//to check if a given number is a palindrome
int check_palindrome_or_not(int);

//to generate the first 'N' prime numbers
int is_prime_number(int);
int generate_first_n_prime_numbers(int);
